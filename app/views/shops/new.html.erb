
<%= form_for @shop do |f|%>
    <%= f. label :user%>
    <%= f.collection_select(:user, User.all, :id, :name) %>

    <%= f.label :item%>
    <%= f.collection_select(:item, Item.all, :id, :name)%>

    <%= f.submit "Put in my bag"%>
<%end%>